import 'package:flutter/material.dart';

class OnSalePage extends StatelessWidget {
  const OnSalePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("On Sale"));
  }
}